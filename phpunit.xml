<?xml version="1.0" encoding="UTF-8"?>
<phpunit
    bootstrap="vendor/autoload.php"
    colors="true"
    beStrictAboutTestsThatDoNotTestAnything="false"
    beStrictAboutOutputDuringTests="false"
>
    <testsuites>
        <testsuite name="Feature">
            <directory suffix="Test.php">tests/Feature</directory>
        </testsuite>
    </testsuites>

    <groups>
        <exclude>
            <group>mysql</group>
        </exclude>
    </groups>

    <php>
        <!-- Default to sqlite for package tests -->
        <env name="APP_ENV" value="testing"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>

        <!-- Enable MySQL tests only when you want -->
        <env name="CRUD_MYSQL_TEST_ENABLED" value="false"/>

        <!-- Optional: explicit MySQL creds for the mysql group -->
        <env name="CRUD_MYSQL_HOST" value="127.0.0.1"/>
        <env name="CRUD_MYSQL_PORT" value="3306"/>
        <env name="CRUD_MYSQL_DATABASE" value="crud_testing"/>
        <env name="CRUD_MYSQL_USERNAME" value="root"/>
        <env name="CRUD_MYSQL_PASSWORD" value=""/>
    </php>
</phpunit>
